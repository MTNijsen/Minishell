signal handler main, signal reset in children done
heredoc

refractor bi_cd
refractor exec
refractor redirect

change functions to return the error code instead of a move to the list node

redo builtins to handle 2d array for envp
write function to add to 2d array
	write realloc
write function to remove from 2d array
write function to retrieve from 2d array
	write split

	//at the start of the proccess we should put the whole envp on the heap so it can all be freed at the end of the process for when we add or remove from it
